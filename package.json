{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "php artisan serve",
    "build": "node -e \"console.log('SobatAnak memakai asset CSS/JS siap pakai dari folder public; tidak ada build Vite root yang diperlukan.')\""
  },
  "engines": {
    "node": "22.x"
  }
}
